<?xml version="1.0" encoding="ISO-8859-1" ?>

<!--

                    This file was generated by Jason 1.3.9-alpha
                    http://jason.sf.net

                    Abril 18, 2014 - 14:35:23
-->

<project name   ="rft"
         basedir="."
         default="run">

    <property name="mas2j.project.file" value="rft.mas2j"/>
    <property name="debug" value=""/> <!-- use "-debug" to run in debug mode -->
    <property name="build.dir" value="${basedir}/bin/classes" />

    <property name="jasonJar" value="../../lib/jason.jar"/>

    <path id="project.classpath">
        <pathelement location="${basedir}"/>
        <pathelement location="${build.dir}"/>
        <pathelement location="${jasonJar}"/>
        <pathelement location="../../lib/jade.jar"/>
        <pathelement location="../../lib/cartago.jar"/>
        <pathelement location="../../lib/c4jason.jar"/>
        <pathelement location="../../lib/moise.jar"/>

    </path>

    <target name="init">
        <mkdir dir="${build.dir}" />
    </target>

    <target name="compile" depends="init">
        <condition property="srcdir" value="${basedir}/src/java" else="${basedir}" >
            <available file="${basedir}/src/java" />
        </condition>
        <javac srcdir="${srcdir}" destdir="${build.dir}" debug="true" optimize="true" includeantruntime="false" >
            <classpath refid="project.classpath"/>
        </javac>
    </target>

    <target name="jar" depends="compile">
        <delete file="${ant.project.name}.jar" />
        <copy file="${jasonJar}" tofile="${ant.project.name}.jar" />
        <copy file="${mas2j.project.file}" tofile="default.mas2j" />
        <jar update="yes" jarfile="${ant.project.name}.jar" >
            <fileset dir="${basedir}">
                <include name="**/*.asl" />
                <include name="**/*.mas2j" />
            </fileset>
            <fileset dir="${build.dir}">
                <include name="**/*.class" />
            </fileset>
            <manifest>
                  <attribute name="Main-Class" value="jason.infra.centralised.RunCentralisedMAS"/>
            </manifest>
        </jar>
        <delete file="default.mas2j" />
    </target>


    <target name="run" depends="compile" >
        <echo message="Running project ${ant.project.name}" />
        <java classname="jason.infra.jade.RunJadeMAS"
              failonerror="true" fork="yes" dir="${basedir}" >
            <classpath refid="project.classpath"/>
            <arg line="${mas2j.project.file} ${debug} "/>
            <!-- jvmarg line="-Xmx500M -Xss8M"/ -->
        </java>
    </target>

    <target name="run-cent" depends="compile" >
        <echo message="Running project ${ant.project.name}" />
        <java classname="jason.infra.centralised.RunCentralisedMAS"
              failonerror="true" fork="yes" dir="${basedir}" >
            <classpath refid="project.classpath"/>
            <arg line="${mas2j.project.file} ${debug} "/>
            <!-- jvmarg line="-Xmx500M -Xss8M"/ -->
        </java>
    </target>

    <target name="clean" >
        <delete failonerror="no" includeEmptyDirs="true" verbose="true">
            <fileset dir="${basedir}" includes="**/*.class"/>
        </delete>
    </target>

    <target name="Main-Container" depends="compile" >
        <echo message="Starting JADE Container Main-Container" />
        <java classname="jade.Boot" failonerror="true" fork="yes" dir="${basedir}" >
            <classpath refid="project.classpath"/>
            <arg line=" -agents  majordomo:jason.infra.jade.JadeAgArch(j-project,rft.mas2j,majordomo);bob:jason.infra.jade.JadeAgArch(j-project,rft.mas2j,bob);alice:jason.infra.jade.JadeAgArch(j-project,rft.mas2j,alice)"/>
            <jvmarg line="-Xmx500M -Xss8M"/>
        </java>
    </target>



</project>
